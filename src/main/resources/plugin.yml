name: ${name}
version: ${version}
description: ${description}
author: ${author}
website: ${url}

main: ${mainClass}

commands:
  kitpvp:
    description: Plugin command
    permission: kitpvp.kitpvp
    usage: /<command>

permissions: {}